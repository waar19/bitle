# ESP-IDF build output
build/
cmake-build-*/
CMakeFiles/
CMakeCache.txt
*.ninja
.ninja_log
.ninja_deps

# ESP-IDF configuration artifacts
sdkconfig
sdkconfig.old
sdkconfig.*.backup
sdkconfig.*.old
dependencies.lock

# Flash images and intermediate binaries
*.bin
*.elf
*.map
flash_args
partition_table/
bootloader/

# Python cache
__pycache__/
*.pyc

# IDE/editor metadata
.vscode/
.idea/
.cache/
.DS_Store

# Logs and scratch
*.log

# OTA: signed manifests are release artifacts; the owner private key must
# never be committed (it lives outside the repo).
*.bota
*bitle_owner_key*
